import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cocktailMasters.app',
  appName: 'Cocktail Master',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
